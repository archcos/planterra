import 'package:flutter/material.dart';

class SellingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Selling Screen', style: TextStyle(fontSize: 24)),
    );
  }
}
